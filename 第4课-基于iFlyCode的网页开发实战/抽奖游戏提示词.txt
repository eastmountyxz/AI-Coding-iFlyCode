请生成一个课堂互动抽奖系统网页，代码为HTML文件（含CSS和JavaScript），UTF-8编码。具体要求如下：
1、学生名单：在JavaScript中预定义一个包含 20 名学生姓名的数组，例如 ["张三","李四","王五",...,"赵二十"]；

2、页面结构：HTML页面需包含三个部分：①转盘区（显示 20 名学生分布在圆形转盘上）；②操作按钮区（一个“开始抽奖”按钮）；③结果显示区（展示中奖者姓名）；

3、转盘动画：CSS 实现转盘旋转动画，点击按钮后转盘旋转约 5 秒随机停止；

4、逻辑控制：JavaScript 使用 Math.random() 生成随机结果，确保公平性；抽中的学生应从数组中移除，保证 不重复抽取；

5、结果展示：中奖者姓名需高亮显示在结果区，并实时更新；

6、交互体验：页面需支持响应式设计，在PC与移动端均可自适应显示；

注释与可读性：请在代码中添加必要的中文注释，保证结构清晰，逻辑易读。
